{
  "name": "spot-canvas",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@8.10.5",
  "scripts": {
    "dev": "pnpm --filter @spot-canvas/web dev",
    "desktop": "pnpm --filter @spot-canvas/desktop dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  }
}
